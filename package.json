{
  "name": "superkus",
  "scripts": {
    "start": "parcel ./index.html",
    "copy-samples": "ncp ./samples ./dist",
    "build": "parcel build ./index.html --out-dir ./build && ncp ./samples ./build"
  },
  "devDependencies": {
    "lite-server": "2.3.0",
    "ncp": "2.0.0",
    "node-sass": "4.9.0",
    "typescript": "2.8.3"
  },
  "dependencies": {
    "tone": "0.12.80"
  }
}
